{
  "name": "creative-studio-saas",
  "version": "1.0.0",
  "description": "A comprehensive SaaS platform for creative professionals in Egypt.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "rimraf dist && mkdir dist && esbuild index.tsx --bundle --outfile=dist/bundle.js --jsx=automatic --loader:.ts=tsx --format=esm --minify && cp index.html styles.css favicon.svg dist/"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "rimraf": "^5.0.5"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
